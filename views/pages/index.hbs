<section class="pages">
<h1>{{ __ "pages" }}</h1>
<ul>
  {{#each contents}}
  <li>
    <h4><a href="/pagess/{{ this.slug }}">{{ this.title }}</a></h4>
    <div class="subtitle">{{formatDate this.createdAt}}</div>
  </li>
  {{/each}}
</ul>
</section>
